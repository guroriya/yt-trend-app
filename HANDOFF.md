# HANDOFF.md — 引き継ぎ

このファイルと `docs/ORDER.md` の2枚だけで再開できる状態を保つ（ORDER §1-2）。
最終更新: 2026-08-25

## 現在地

- **P0 完了。P1（収集）・P3（v1 UI）・P4（v2 UI）はコード完成。実走だけが人間ゲート待ち。**
  4タブ（みんな／自分／ワード／世界）、4軸（期間・部門・カテゴリ・国）、スワイプザッピング、
  順位変動↑↓NEW、8件ごと広告枠（差し替えフック付き）、i18n、ライト/ダーク、PWA、
  学習インスペクタ（見える・消せる・いじれる）まで、モックデータ上で実測確認済み。
- 未実行コードを2系統の独立レビューに掛け、確認された不具合（割当の計上漏れ、
  カウンタの巻き戻り、SW の版が固定、テーマ先読みの JSON 不一致 ほか計20件）を修正済み。
- デザイン憲章の機械チェック（`tools/audit.js`）は **英日 × ライト/ダーク × 全8ビュー × 2画面幅**
  で指摘ゼロ。スキーマ検証も生成データ 44 ファイルで エラーゼロ。360×800 の初見件数は 6〜7件
  （憲章の下限は4件）。
- 検査そのものも強化した: `owns()` の祖先合格を外した途端に `.btn-ghost` の当たり判定不足が出て、
  `[hidden]` の新ルールで「隠したはずのカテゴリチップと指標行が出たままだった」不具合が出た。

## 次の一手

1. **発注者に `NEEDS_HUMAN.md` のゲート0（Node/gh 導入）→ ゲートA（APIキー）→ ゲートE（repo/Pages）を実行してもらう。**
2. ゲート0 が解けたら `npm ci && npx playwright install --with-deps chromium && npm run test:e2e` で
   E2E を実走（P0/P3/P4 の検収）。`npm run collect -- --dry-run` で予算計画も目視できる。
3. 実データが 3 日ぶん貯まると「伸び」ランキングが自動で有効化される（`--growth` で先に見た目だけ確認可）。

## 未解決

- ローカルに Node/npm/gh が無いため、**収集スクリプトと Playwright は一度も実走していない**
  （純粋ロジックの `plan.mjs` / `pure.mjs` / `tags.mjs` / `schema.mjs` はブラウザで実行して検証済み）。
- この環境のブラウザは **Service Worker 登録と IntersectionObserver の発火ができない**
  （埋め込みブラウザ側の制約）。PWA オフラインと表示ログ学習の実動確認は CI の E2E に委ねている。
- 世界地図の地形は簡易グリッド。実測ジオメトリ差し替えは `BACKLOG.md`。

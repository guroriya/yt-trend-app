# HANDOFF.md — 引き継ぎ

このファイルと `docs/ORDER.md` の2枚だけで再開できる状態を保つ（ORDER §1-2）。
最終更新: 2026-08-25

## 現在地

- **P0 完了。P3(v1 UI) と P4(v2 UI) の実装はモックデータ上でほぼ完成、P1(収集) のコードも完成。**
  「みんな／自分／ワード／世界」4タブ、期間・部門・カテゴリ・国の4軸、スワイプザッピング、
  順位変動↑↓NEW、8件ごと広告枠、1位ヒーロー、i18n(en/ja)、ライト/ダーク、PWA、学習インスペクタまで動く。
- 収集側は `scripts/`（planner・API・ショート判定・スナップショット・タグ・スキーマ検証）まで実装済み。
  予算プランナはブラウザ実測で `docs/BUDGET.md` の数値と一致（10k=7,125 / 15k=10,357 / 20k=13,589 / 30k=23,285）。
- デザイン憲章の機械チェック（`tools/audit.js`）は 360×800 と 412×915 × ライト/ダーク × 全タブで **指摘ゼロ**。

## 次の一手

1. **発注者に `NEEDS_HUMAN.md` のゲート0（Node/gh 導入）とゲートA（APIキー）を実行してもらう。**
   ゲート0 が解けたら `npm ci && npx playwright install chromium && npm run test:e2e` で E2E を実走させる。
2. ゲートE（`gh repo create` → Pages を GitHub Actions に切替）で P2 の検収まで進む。
3. 実データが入ったら「伸び」ランキングが自動有効化されるまで 3 日ほど待ち、P4 の検収を締める。

## 未解決

- ローカルに Node/npm/gh が無いため、**収集スクリプトと Playwright は一度も実走していない**
  （純粋ロジックの `plan.mjs` / `schema.mjs` はブラウザで実行して検証済み）。
- ブラウザのスクリーンショットが撮れない環境のため、`design-reviewer` は数値監査ベースで運用中。
  発注者のスクショ指摘（ゲートD）はいつでも歓迎で、憲章より優先して反映する。
- 世界地図の地形は簡易グリッド。実測ジオメトリ差し替えは `BACKLOG.md`。

# BACKLOG.md — 積み残し

ORDER §1-4（5回リトライで通らなかった項目）と、デザイン監査の Nice 指摘、
および将来やるが今はやらないことを理由付きで積む。**全体は止めない。**

書式: `- [ ] 内容 — 理由 / 前提条件`

---

## 環境・インフラ
- [ ] ローカル Playwright 実行 — ローカルに Node が無いため CI 実行に寄せた（NEEDS_HUMAN ゲート0 が解けたら解消）
- [ ] `gh run watch` による P2 検収 — gh CLI が無い。GitHub の Actions 画面で目視確認に代替（ゲート0/ゲートE）

## 運用
- [ ] **GitHub は 60 日リポジトリ活動が無いと定期実行（cron）を自動で止める。** このアプリは収集結果をコミットしない設計（ORDER §8）なので、放置すると自動的に活動ゼロになりうる — GitHub から届く通知メールで再有効化するか、月1回何かを push する運用にする。無害な keepalive ワークフローを足す案もあるが、まずは通知運用で足りるか様子を見る
- [ ] `state/` の Actions キャッシュは 7 日参照されないと消える。長く止めると `prev/`（順位変動の比較元）が失われ、全件 NEW 表示になる（自然に回復する）

## 機能
- [ ] 世界地図の地形は簡易 SVG。実測ジオメトリ（簡略 TopoJSON 埋め込み）への差し替え — v2 の価値は「各国1位への潜り込み」であり地形精度は二次。外部 CDN 禁止のため埋め込みサイズと相談
- [ ] 「伸び」ランキング（ORDER §2-14）は初期は自動非表示 — スナップショットの蓄積日数が足りるまで有効化できない仕様。蓄積後に自動有効化される実装済み

## デザイン監査の積み残し（ORDER §1-6: 3巡で残った非 Critical 指摘）
2026-08-25 の憲章監査（3観点 × 反証つき）で出た22件のうち、直さずに残したもの。
- [ ] サムネイルが行の高さを決めていない — 行 92px に対し画像は 63px。行高は情報列（2行タイトル）が決めており、
      「サムネ主役」を額面どおりにするなら画像を高さ基準にするレイアウト変更が要る（＝カード設計のやり直し）
- [ ] 360〜559px の間でサムネ幅が 112px に固定で、幅が広がるほど画像の占有比が下がる
- [ ] 順位数字が3つの焦点（数字・サムネ・タイトル）の中でいちばん弱い（色が --text-2）。
      強めると賑やかになりすぎる懸念があり、発注者のスクショ指摘（ゲートD）を待って判断したい
- [x] ワードの棒グラフがブランドマークと同じグラデーション → 2026-08-25 解消。量を表す単色（accent の淡色）に分離
- [x] チップをタップしたときは zap アニメーションが出ない → 2026-08-25 解消。期間・部門・カテゴリのチップと国トグルにも方向つき zap を追加
- [ ] タップ集計の「今日」は UTC 基準（日本では毎朝9時に切り替わって見える）。ユーザー向け注記を出すかは実データが付いてから判断（レビュー 2026-08-25 Nice）
- [ ] GB と US は同じ検索語（`SEARCH_Q.en`）を使うため、ランキングが似通う可能性がある — regionCode は効いているはずだが、GB の実データが貯まってから重複率を測って判断する（2026-08-25 レビュー）
- [ ] `planner` が skipped にしたジョブは `--jobs` でも `--force` でも動かせない（`isDue` より前に弾かれる）— 構成を絞れば起きないが、手動で1本だけ取り直したいときに困る


# DECISIONS.md — 判断ログ

ORDER §1-1 に従い、発注者に確認せず自己解決した判断を1行ずつ追記する。**追記のみ。削除しない。**

書式: `YYYY-MM-DD | 分野 | 判断 | 理由（最も安く後戻りできる根拠）`

---

- 2026-08-25 | repo | リポジトリ名は `yt-trend-app` のまま、場所は `C:\Users\Owner\yt-trend-app` | 既存プロジェクト（pov_wrestle 等）がユーザーホーム直下に並ぶ慣例に合わせた
- 2026-08-25 | env | ローカルに Node/npm/gh CLI が無い。**インストールを勝手に実行せず** NEEDS_HUMAN.md ゲート0 に記載し、Node 非依存の経路で進める | 環境の書き換えは後戻りコストが高い。収集は Actions 上の Node 20 で走るのでローカル Node は必須ではない
- 2026-08-25 | tooling | ローカル配信は `python -m http.server`、モック生成は Python、視覚確認はブラウザ自動操作 | Python 3.10 は既に入っている。Playwright は CI で実行し、仕様は `tests/` に置く
- 2026-08-25 | ui | UI は `public/index.html` / `public/app.css` / `public/app.js` の3枚に集約（i18n 辞書と config のみ分離） | ORDER §3「発注者が単一ファイルの差分ループで手を入れられる構成を優先」
- 2026-08-25 | data | ランキング JSON はフラット命名 `data/{country}-{section}-{period}-{category}.json`＋`data/index.json` | Service Worker のキャッシュ列挙とプリフェッチが単純になる。階層より後戻りが安い
- 2026-08-25 | data | サムネ URL は JSON に持たず `videoId` から生成（`https://i.ytimg.com/vi/{id}/mqdefault.jpg`） | JSON サイズを約20%削減。i.ytimg.com の URL 規約は安定
- 2026-08-25 | budget | ORDER §4 の「24h＝毎時」は既定割当 10,000 units/日に収まらない（毎時だと 19,392 units/日）。**毎時 cron で起動し、予算プランナが自動で実効間隔を落とす**構成にした | ORDER §4 の「8,000 units 超で自動的に更新頻度を落とすセーフガード」を第一級の設計として実装。詳細は docs/BUDGET.md
- 2026-08-25 | budget | 総合（category=all）は上位100件（search 2ページ）、カテゴリ別タブは上位50件（1ページ） | 100件×全カテゴリは割当に収まらない。カテゴリ別は ORDER §2-3 で「割当の都合でまず24時間のみ」とされており、件数も同じ理由で削るのが最も安い
- 2026-08-25 | shorts | ショート判定は「長さ≤3分」を候補にし、`/shorts/{id}` の到達性で確定。結果は `_shorts_cache.json` に30日 TTL で保存 | ORDER §2-2。HTTP 確認は API 割当を消費しない。失敗時は長さヒューリスティックにフォールバック（数%の誤判定は許容）
- 2026-08-25 | i18n | 言語は `en` を既定、`?lang=` → localStorage → `navigator.language` の順で決定 | ORDER §0「英語ベース＋日本語切替」。世界展開前提のため既定は英語
- 2026-08-25 | data | 内部状態（予算カウンタ・ショート判定キャッシュ・前回順位・スナップショット）は `public/data/` ではなく `state/` に置く | 公開ディレクトリに内部ファイルを混ぜない。Actions では `actions/cache` で引き継ぐ
- 2026-08-25 | data | **`public/data/` と `state/` を .gitignore に入れ、生成物は一切コミットしない** | ORDER §8「取得した API データの保存は30日以内にリフレッシュまたは削除」。git 履歴に入れると削除できなくなる。配信前に `npm run collect`（キーがあれば）または `python tools/mock.py` で必ず生成する
- 2026-08-25 | ui | アプリ名は **TrendZap**（表示名・PWA名・タイトル） | ORDER に名称指定がないため自己決定。「ザッピング」という体験の核を名前にした。YouTube を模倣・僭称しない（ORDER §8）
- 2026-08-25 | ui | ショート部門もサムネイルは 16:9 のまま並べ、左肩の縦バッジで部門を示す | 9:16 にすると 360×800 の1画面に3件しか入らず憲章「一覧性が正義／最低4件」に反する。i.ytimg のサムネ自体が 16:9
- 2026-08-25 | ui | アプリバーからテーマ切替ボタンを外し設定シートへ移動 | 360px 幅でアプリバーが窮屈になり、テーマ切替は頻度が低い。言語切替だけ残した（世界展開で「言語を戻せない」事故を防ぐため）
- 2026-08-25 | ui | 地図のピンは 44px を確保できない（欧州で必ず重なる）ため副次導線とし、地図の下に 44px の国リストを主導線として置く | 憲章のタップ目標を満たす経路を必ず1本用意する。ピンにはクラスタリングを入れない（費用対効果が低い）
- 2026-08-25 | tooling | `tools/audit.js`（憲章の機械チェック）と `tools/devserve.py`（.mjs を配る開発サーバー）を用意 | スクリーンショットが撮れない環境でも、密度・タップ目標・コントラスト・横あふれは数値で判定できる。純粋関数モジュール（plan.mjs / schema.mjs）はブラウザで実行して検証した
- 2026-08-25 | tooling | `python tools/mock.py --growth` で「伸び」ランキングのサンプルも生成できるようにした | ORDER §2-14 の自動有効化（index.json の features.growth）を実データを待たずに UI/E2E で確認するため。既定（フラグ無し）は enabled:false ＝ 実際の初期状態と同じ
- 2026-08-25 | env | この環境のブラウザでは Service Worker の登録自体ができない（`An unknown error occurred when fetching the script`。HTTP/1.1・MIME 正常でも同じ）。PWA／オフラインの実動確認は CI の Playwright に委ねる | 埋め込みブラウザ側の制約でコードの問題ではない。登録タイミングのバグ（load 発火後に addEventListener していた）はこの調査で見つけて修正済み
- 2026-08-25 | env | この環境のブラウザは Service Worker 登録も IntersectionObserver の発火もできない（ペインが描画されていないため）。PWA オフラインと「表示ログ学習」の実動確認は CI の Playwright に委ねる | 埋め込みブラウザ側の制約。コードの問題ではないことを、手動 `Learn.record()` が正しく動くことで切り分けた
- 2026-08-25 | ci | Service Worker の `VERSION` は公開直前に CI がコミット SHA へ書き換える | ビルド工程が無い（ORDER §3）ため、固定値のままだと本番の cache-first シェルが二度と更新されない。sed 1行で済み、開発時の手編集も妨げない
- 2026-08-25 | qa | 未実行コードは「独立レビュー → 各指摘を別エージェントが反証」の二段で検証し、反証できなかったものだけ直す | ローカルで走らせられない以上、机上レビューの精度を上げるしかない。実際に 26 件中 5 件が確認され、残りは誤読・修正済みだった
- 2026-08-25 | ci | Actions のキャッシュを `state`（割当カウンタ等）と `public/data`（公開する集計結果）の2本に分けた | 保存条件が違う。state は検証が落ちても always() で保存（消費した割当は取り返せない）、public/data はスキーマ検証を通ったときだけ保存（壊れたデータを焼き付けない）
- 2026-08-25 | ci | deploy.yml は公開前にキャッシュから public/data を復元し、無いときだけサンプルを生成する | そうしないと UI の小さな修正を push するたびに、公開中の実データがサンプルに戻る
- 2026-08-25 | ci | 保持期間の掃除を `scripts/retention.mjs` として切り出し、APIキーの有無に関わらず毎回実行する | ORDER §8 の30日制約はキーが外れていても守る必要がある。中身は `scripts/lib/retention.mjs` で収集本体と共用
- 2026-08-25 | qa | ORDER §5 のデザイン監査は `design-reviewer` サブエージェント**型**がこのセッションのレジストリに出てこなかったため、同じ指示文を汎用エージェントに読ませて3観点（序列／祭りと変動の瞬読／英日とダーク）で実行し、各指摘を別エージェントが反証する形にした | ORDER の意図（憲章に照らした独立監査）は満たしている。指摘22件のうち反証を突破した3件＋明らかに実在する10件を修正し、残りは BACKLOG（ORDER §1-6）
- 2026-08-25 | ui | `--gold-fill`（#ffc247 固定）を廃し、テーマ可変の `--gold` に一本化 | 白背景で 1.61:1 しかなく、モードタブの下線の右半分が見えていなかった。ライトは #b47500（3.83:1）、ダークは #ffc247（11.9:1）
- 2026-08-25 | ui | 「自分」タブでは順位変動マークを出さない | 前回順位という概念が無く、全100行が NEW になって変動の合図そのものが意味を失っていた（憲章「順位変動は色で瞬読できる」の逆効果）
- 2026-08-25 | ads | 広告枠の挿入間隔を8件→**10件ごと**に変更（`AD_EVERY=10`） | 発注者のチャット指示（「使いやすさを考え、煩わしくなく、それでも表示」）。発注者指示は ORDER より優先。ORDER §2-9 に改訂注記を入れた
- 2026-08-25 | v3 | ゲート0/A/E 待ちの間、P5（匿名タップ集計）をコード先行で実装（デプロイのみゲートB） | ORDER §1-5/§7「ゲートに依存しない作業を続ける」。P1/P3/P4 と同じ「コード完成・実走待ち」方式
- 2026-08-25 | v3 | 独自指標は「◯人」でなく「◯回」表記 | 匿名制約（ORDER §2-15: 個人識別子の保存不可）の下では人数の一意化が原理的に不可能。誇張表示を避ける
- 2026-08-25 | v3 | タップ送信は content-type を付けない fetch（CORS 単純リクエスト） | プリフライト無しで 1タップ=1リクエスト。KV 無料枠（1,000書込/日）と応答性の両得。サーバーは本文 JSON だけを見る
- 2026-08-25 | v3 | KV の read-modify-write 競合による取りこぼしは許容（SCHEMA.md に明記） | 独自指標の近似値であり課金根拠にしない。正確さが要る段階で Durable Objects へ
- 2026-08-25 | v3 | `?taps=mock` はビルトインのサンプル統計のみ。URL の外部エンドポイント指定は不可 | 悪意あるリンクで任意サーバーへ送信させられる注入経路を作らない。実エンドポイントは config.js の1箇所だけ
- 2026-08-25 | docs | P6 の申請文・ストア掲載文・データセーフティ回答を docs/SUBMISSIONS.md に先行整備 | ORDER §7 ゲートC「発注者は提出操作のみ」。審査待ち期間が長いので文面だけ先に固める
- 2026-08-25 | qa | 本セッションの変更（v3・広告10件・BACKLOG修正）も「独立レビュー3観点→指摘ごとに反証」で検証。18指摘中13件が確認され修正、5件は反証で棄却 | 前回と同じ二段方式（`5c8b41c`）。CORS の Origin:null fail-open と e2e の endpoint 固定依存（ゲートB後に CI 全滅）は机上レビューだけでは出なかった
- 2026-08-25 | v3 | 地図ピンのタップ数バッジは「上位5か国＋対応国」だけに限定 | 欧州クラスタでバッジ同士が重なり数字が誤読される（監査で座標実測）。全数値は国リスト（主導線）に必ず出るので情報は失われない
- 2026-08-25 | i18n | 英語のタップ数文言はラベル形（"Zaps …: {n}" / "{n}× today"）にして単複を回避 | t() に複数形分岐が無く "1 zaps" になるため。日本語は既存の助数詞密着流儀（{n}回）に統一
- 2026-08-25 | privacy | 発注者指摘により全コミットの作者を guroriya＋GitHub匿名メールへ書き換えて強制push（旧設定の個人名とGmailが公開されていた） | リポジトリ公開直後・他利用者ゼロのため履歴書き換えが最も安全。このリポジトリの user.name/email もローカル設定済み
- 2026-08-25 | env | ゲート0・E 完了（Node v24.19.0 / gh 2.98.0 / repo公開 / Pages有効化）。E2E 初実走 182/182 緑（テスト側の誤り2件は修正） | P0 検収の「Playwrightスモーク通過」達成。残る人間ゲートは A（APIキー）のみで v1 実データ化
- 2026-08-25 | collect | **search.list は q なしだと常に 0 件を返す**（CI 上の実測プローブで確認。400「API key not valid」はキー再登録で解消済みの別問題）。q は事実上コンテンツ言語を決めるため、言語別の常用語 OR（ja=の|に|を|が|は / en=a|the|i|to）を `SEARCH_Q` として config.js に追加し全 search に渡す | JP+q=の は日本の動画だけを高再生順で返すことを実測確認。これらの語を含まない動画の取りこぼしは数%許容（ショート判定と同格）。並びの粗さは従来どおり videos.list の実数で再整列して吸収


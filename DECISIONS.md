# DECISIONS.md — 判断ログ

ORDER §1-1 に従い、発注者に確認せず自己解決した判断を1行ずつ追記する。**追記のみ。削除しない。**

書式: `YYYY-MM-DD | 分野 | 判断 | 理由（最も安く後戻りできる根拠）`

---

- 2026-08-25 | repo | リポジトリ名は `yt-trend-app` のまま、場所は `C:\Users\Owner\yt-trend-app` | 既存プロジェクト（pov_wrestle 等）がユーザーホーム直下に並ぶ慣例に合わせた
- 2026-08-25 | env | ローカルに Node/npm/gh CLI が無い。**インストールを勝手に実行せず** NEEDS_HUMAN.md ゲート0 に記載し、Node 非依存の経路で進める | 環境の書き換えは後戻りコストが高い。収集は Actions 上の Node 20 で走るのでローカル Node は必須ではない
- 2026-08-25 | tooling | ローカル配信は `python -m http.server`、モック生成は Python、視覚確認はブラウザ自動操作 | Python 3.10 は既に入っている。Playwright は CI で実行し、仕様は `tests/` に置く
- 2026-08-25 | ui | UI は `public/index.html` / `public/app.css` / `public/app.js` の3枚に集約（i18n 辞書と config のみ分離） | ORDER §3「発注者が単一ファイルの差分ループで手を入れられる構成を優先」
- 2026-08-25 | data | ランキング JSON はフラット命名 `data/{country}-{section}-{period}-{category}.json`＋`data/index.json` | Service Worker のキャッシュ列挙とプリフェッチが単純になる。階層より後戻りが安い
- 2026-08-25 | data | サムネ URL は JSON に持たず `videoId` から生成（`https://i.ytimg.com/vi/{id}/mqdefault.jpg`） | JSON サイズを約20%削減。i.ytimg.com の URL 規約は安定
- 2026-08-25 | budget | ORDER §4 の「24h＝毎時」は既定割当 10,000 units/日に収まらない（毎時だと 19,392 units/日）。**毎時 cron で起動し、予算プランナが自動で実効間隔を落とす**構成にした | ORDER §4 の「8,000 units 超で自動的に更新頻度を落とすセーフガード」を第一級の設計として実装。詳細は docs/BUDGET.md
- 2026-08-25 | budget | 総合（category=all）は上位100件（search 2ページ）、カテゴリ別タブは上位50件（1ページ） | 100件×全カテゴリは割当に収まらない。カテゴリ別は ORDER §2-3 で「割当の都合でまず24時間のみ」とされており、件数も同じ理由で削るのが最も安い
- 2026-08-25 | shorts | ショート判定は「長さ≤3分」を候補にし、`/shorts/{id}` の到達性で確定。結果は `_shorts_cache.json` に30日 TTL で保存 | ORDER §2-2。HTTP 確認は API 割当を消費しない。失敗時は長さヒューリスティックにフォールバック（数%の誤判定は許容）
- 2026-08-25 | i18n | 言語は `en` を既定、`?lang=` → localStorage → `navigator.language` の順で決定 | ORDER §0「英語ベース＋日本語切替」。世界展開前提のため既定は英語
- 2026-08-25 | data | 内部状態（予算カウンタ・ショート判定キャッシュ・前回順位・スナップショット）は `public/data/` ではなく `state/` に置く | 公開ディレクトリに内部ファイルを混ぜない。Actions では `actions/cache` で引き継ぐ
- 2026-08-25 | data | **`public/data/` と `state/` を .gitignore に入れ、生成物は一切コミットしない** | ORDER §8「取得した API データの保存は30日以内にリフレッシュまたは削除」。git 履歴に入れると削除できなくなる。配信前に `npm run collect`（キーがあれば）または `python tools/mock.py` で必ず生成する
- 2026-08-25 | ui | アプリ名は **TrendZap**（表示名・PWA名・タイトル） | ORDER に名称指定がないため自己決定。「ザッピング」という体験の核を名前にした。YouTube を模倣・僭称しない（ORDER §8）
- 2026-08-25 | ui | ショート部門もサムネイルは 16:9 のまま並べ、左肩の縦バッジで部門を示す | 9:16 にすると 360×800 の1画面に3件しか入らず憲章「一覧性が正義／最低4件」に反する。i.ytimg のサムネ自体が 16:9
- 2026-08-25 | ui | アプリバーからテーマ切替ボタンを外し設定シートへ移動 | 360px 幅でアプリバーが窮屈になり、テーマ切替は頻度が低い。言語切替だけ残した（世界展開で「言語を戻せない」事故を防ぐため）
- 2026-08-25 | ui | 地図のピンは 44px を確保できない（欧州で必ず重なる）ため副次導線とし、地図の下に 44px の国リストを主導線として置く | 憲章のタップ目標を満たす経路を必ず1本用意する。ピンにはクラスタリングを入れない（費用対効果が低い）
- 2026-08-25 | tooling | `tools/audit.js`（憲章の機械チェック）と `tools/devserve.py`（.mjs を配る開発サーバー）を用意 | スクリーンショットが撮れない環境でも、密度・タップ目標・コントラスト・横あふれは数値で判定できる。純粋関数モジュール（plan.mjs / schema.mjs）はブラウザで実行して検証した
- 2026-08-25 | tooling | `python tools/mock.py --growth` で「伸び」ランキングのサンプルも生成できるようにした | ORDER §2-14 の自動有効化（index.json の features.growth）を実データを待たずに UI/E2E で確認するため。既定（フラグ無し）は enabled:false ＝ 実際の初期状態と同じ
- 2026-08-25 | env | この環境のブラウザでは Service Worker の登録自体ができない（`An unknown error occurred when fetching the script`。HTTP/1.1・MIME 正常でも同じ）。PWA／オフラインの実動確認は CI の Playwright に委ねる | 埋め込みブラウザ側の制約でコードの問題ではない。登録タイミングのバグ（load 発火後に addEventListener していた）はこの調査で見つけて修正済み
- 2026-08-25 | env | この環境のブラウザは Service Worker 登録も IntersectionObserver の発火もできない（ペインが描画されていないため）。PWA オフラインと「表示ログ学習」の実動確認は CI の Playwright に委ねる | 埋め込みブラウザ側の制約。コードの問題ではないことを、手動 `Learn.record()` が正しく動くことで切り分けた
- 2026-08-25 | ci | Service Worker の `VERSION` は公開直前に CI がコミット SHA へ書き換える | ビルド工程が無い（ORDER §3）ため、固定値のままだと本番の cache-first シェルが二度と更新されない。sed 1行で済み、開発時の手編集も妨げない
- 2026-08-25 | qa | 未実行コードは「独立レビュー → 各指摘を別エージェントが反証」の二段で検証し、反証できなかったものだけ直す | ローカルで走らせられない以上、机上レビューの精度を上げるしかない。実際に 26 件中 5 件が確認され、残りは誤読・修正済みだった
- 2026-08-25 | ci | Actions のキャッシュを `state`（割当カウンタ等）と `public/data`（公開する集計結果）の2本に分けた | 保存条件が違う。state は検証が落ちても always() で保存（消費した割当は取り返せない）、public/data はスキーマ検証を通ったときだけ保存（壊れたデータを焼き付けない）
- 2026-08-25 | ci | deploy.yml は公開前にキャッシュから public/data を復元し、無いときだけサンプルを生成する | そうしないと UI の小さな修正を push するたびに、公開中の実データがサンプルに戻る
- 2026-08-25 | ci | 保持期間の掃除を `scripts/retention.mjs` として切り出し、APIキーの有無に関わらず毎回実行する | ORDER §8 の30日制約はキーが外れていても守る必要がある。中身は `scripts/lib/retention.mjs` で収集本体と共用
- 2026-08-25 | qa | ORDER §5 のデザイン監査は `design-reviewer` サブエージェント**型**がこのセッションのレジストリに出てこなかったため、同じ指示文を汎用エージェントに読ませて3観点（序列／祭りと変動の瞬読／英日とダーク）で実行し、各指摘を別エージェントが反証する形にした | ORDER の意図（憲章に照らした独立監査）は満たしている。指摘22件のうち反証を突破した3件＋明らかに実在する10件を修正し、残りは BACKLOG（ORDER §1-6）
- 2026-08-25 | ui | `--gold-fill`（#ffc247 固定）を廃し、テーマ可変の `--gold` に一本化 | 白背景で 1.61:1 しかなく、モードタブの下線の右半分が見えていなかった。ライトは #b47500（3.83:1）、ダークは #ffc247（11.9:1）
- 2026-08-25 | ui | 「自分」タブでは順位変動マークを出さない | 前回順位という概念が無く、全100行が NEW になって変動の合図そのものが意味を失っていた（憲章「順位変動は色で瞬読できる」の逆効果）
- 2026-08-25 | ads | 広告枠の挿入間隔を8件→**10件ごと**に変更（`AD_EVERY=10`） | 発注者のチャット指示（「使いやすさを考え、煩わしくなく、それでも表示」）。発注者指示は ORDER より優先。ORDER §2-9 に改訂注記を入れた
- 2026-08-25 | v3 | ゲート0/A/E 待ちの間、P5（匿名タップ集計）をコード先行で実装（デプロイのみゲートB） | ORDER §1-5/§7「ゲートに依存しない作業を続ける」。P1/P3/P4 と同じ「コード完成・実走待ち」方式
- 2026-08-25 | v3 | 独自指標は「◯人」でなく「◯回」表記 | 匿名制約（ORDER §2-15: 個人識別子の保存不可）の下では人数の一意化が原理的に不可能。誇張表示を避ける
- 2026-08-25 | v3 | タップ送信は content-type を付けない fetch（CORS 単純リクエスト） | プリフライト無しで 1タップ=1リクエスト。KV 無料枠（1,000書込/日）と応答性の両得。サーバーは本文 JSON だけを見る
- 2026-08-25 | v3 | KV の read-modify-write 競合による取りこぼしは許容（SCHEMA.md に明記） | 独自指標の近似値であり課金根拠にしない。正確さが要る段階で Durable Objects へ
- 2026-08-25 | v3 | `?taps=mock` はビルトインのサンプル統計のみ。URL の外部エンドポイント指定は不可 | 悪意あるリンクで任意サーバーへ送信させられる注入経路を作らない。実エンドポイントは config.js の1箇所だけ
- 2026-08-25 | docs | P6 の申請文・ストア掲載文・データセーフティ回答を docs/SUBMISSIONS.md に先行整備 | ORDER §7 ゲートC「発注者は提出操作のみ」。審査待ち期間が長いので文面だけ先に固める
- 2026-08-25 | qa | 本セッションの変更（v3・広告10件・BACKLOG修正）も「独立レビュー3観点→指摘ごとに反証」で検証。18指摘中13件が確認され修正、5件は反証で棄却 | 前回と同じ二段方式（`5c8b41c`）。CORS の Origin:null fail-open と e2e の endpoint 固定依存（ゲートB後に CI 全滅）は机上レビューだけでは出なかった
- 2026-08-25 | v3 | 地図ピンのタップ数バッジは「上位5か国＋対応国」だけに限定 | 欧州クラスタでバッジ同士が重なり数字が誤読される（監査で座標実測）。全数値は国リスト（主導線）に必ず出るので情報は失われない
- 2026-08-25 | i18n | 英語のタップ数文言はラベル形（"Zaps …: {n}" / "{n}× today"）にして単複を回避 | t() に複数形分岐が無く "1 zaps" になるため。日本語は既存の助数詞密着流儀（{n}回）に統一
- 2026-08-25 | privacy | 発注者指摘により全コミットの作者を guroriya＋GitHub匿名メールへ書き換えて強制push（旧設定の個人名とGmailが公開されていた） | リポジトリ公開直後・他利用者ゼロのため履歴書き換えが最も安全。このリポジトリの user.name/email もローカル設定済み
- 2026-08-25 | env | ゲート0・E 完了（Node v24.19.0 / gh 2.98.0 / repo公開 / Pages有効化）。E2E 初実走 182/182 緑（テスト側の誤り2件は修正） | P0 検収の「Playwrightスモーク通過」達成。残る人間ゲートは A（APIキー）のみで v1 実データ化
- 2026-08-25 | collect | **search.list は q なしだと常に 0 件を返す**（CI 上の実測プローブで確認。400「API key not valid」はキー再登録で解消済みの別問題）。q は事実上コンテンツ言語を決めるため、言語別の常用語 OR（ja=の|に|を|が|は / en=a|the|i|to）を `SEARCH_Q` として config.js に追加し全 search に渡す | JP+q=の は日本の動画だけを高再生順で返すことを実測確認。これらの語を含まない動画の取りこぼしは数%許容（ショート判定と同格）。並びの粗さは従来どおり videos.list の実数で再整列して吸収
- 2026-08-25 | scope | 発注者改訂（第2弾）: ①タブ名 みんな→ランキング ②KR/GB 追加で4カ国 ③カテゴリを全期間に ④ランキング内検索＋期間つき YouTube 転送 ⑤よく見るランキング（自動＋☆） ⑥世界地図の地形と各国代表の重複解消 ⑦ワードタブ→検索タブ | ORDER §2 に改訂注記を入れて正本を更新。発注者指示は ORDER より優先（CLAUDE.md §2）
- 2026-08-25 | budget | 4カ国＋カテゴリ全期間は既定割当 10,000 units に収まるが、planner が更新間隔を落とす（top24h 6h→8h、カテゴリ 日次→7日）。docs/BUDGET.md を全面更新 | ORDER §4 のセーフガードが設計どおり機能。理想の「24h＝毎時」には 10万 units 級が必要（増枠は無料申請のみ・有料メニューは存在しない）
- 2026-08-25 | budget | planner に「落としすぎたら安いジョブを最低頻度で復活させる」段を追加 | priority 順に落とすと 26 units の map が重いジョブの巻き添えで消えた。空き枠に収まるなら復活させるほうが常に得
- 2026-08-25 | ui | ワードタブは廃止せず「検索タブ」に作り替え、旧ワードランキングは検索の入口（話題のワードのチップ）として残した | ORDER §2-12 の価値（自前のワード集計）を捨てずに発注者の要望（検索タブ化）を満たす最も安い形
- 2026-08-25 | ui | 検索は2段構え: ランキングタブ＝表示中のリストを絞る / 検索タブ＝集めた全ランキングを横断。どちらも「同じ語＋同じ期間で YouTube 本体へ」の導線つき | YouTube 全体の検索は API キーをブラウザに置けないため不可能（ORDER §8 の転送導線として実装）
- 2026-08-25 | ui | ☆（ピン留め）は検索バーの中に置き、よく見るチップ行はチップがある時だけ出す | 行を2本増やすと 360×800 でファーストビューが3件に落ち、憲章「最低4件」に反した。1行に畳んで4件を維持
- 2026-08-25 | data | 地図の各国代表は「その国の人気チャート1位」＋既出動画は次点へ譲る方式に変更 | 従来は上位5件から絶対再生数最大を選んでいたため、世界的にバズった同じ動画が9か国の代表になっていた（発注者のスクショ指摘）
- 2026-08-25 | qa | 改訂 第2弾も4観点レビュー＋反証に掛けた（セッション上限で反証17件は未完了。Critical 5件は自分で実地検証してから修正） | 未検証の指摘も、実測で再現できたものだけを直す方針は変えない
- 2026-08-25 | ui | index.json の `datasets` を「データ可用性の正本」にし、未収集の軸はチップを隠す／直リンクは近い軸へ寄せる／それでも無ければ「集計中」を出す | 収集は予算内で数日かけて埋まる設計なので、config に定義がある＝データがある、ではない。404 を「通信を確認してください」と出すのは利用者に対して嘘になる
- 2026-08-25 | collect | 再開カーソル（途中で予算切れしたジョブは次回そこから）と、エイジング（待たされた度合いで実行順を決める）を追加 | 1回 8,080 units のジョブは1日の予算に収まらず、従来は毎回先頭からやり直して末尾に永久に到達しなかった。60日シミュレーションで 240本中96本が欠落 → 対策後は3日で全240本。テストで固定（tests/unit-lib.spec.js）
- 2026-08-25 | ui | 「YouTubeで検索」ボタンを検索バーからステータスバーへ移した | 1文字入力した瞬間にボタンが現れて入力欄が 266px→44px に潰れていた（日本語で1〜2文字しか見えない）。移動後は216px を維持
- 2026-08-25 | ui | 地図の陸は「面の色」ではなく「輪郭線」で読ませる（--land-edge を海に対して 3.1:1 / ダーク 3.6:1、線幅 1.2px） | 面だけで 3:1 を取ると地図が塗り絵のように重くなり、ピンとサムネの視認性を奪う。WCAG 1.4.11 は輪郭で満たすほうが目的に合う
- 2026-08-25 | data | tags の母集団を videoId で一意化してから数える | カテゴリ全期間化で同じ動画が最大10本のリストに載るようになり、露出の多い動画の語だけが重く効いていた
- 2026-08-25 | data | 地図が部分的にしか取れなかった実行では、公開中の前回データで欠けた国を埋めてから書く | 予算切れの実行が26カ国の地図を数カ国に細らせるのを防ぐ（古い行が混じるほうが虫食いよりまし）
- 2026-08-25 | collect | API を使わない後工程（スナップショット・伸び・ワード集計）を、収集本体とは別の try に分けた | 予算切れの QuotaExceededError で丸ごと飛ばされ、本番の `tags-{country}.json` が 404 のままだった（実データで発覚）。予算を使い切る日がある設計（BUDGET.md）なので、0 units の後工程が巻き添えで止まってはいけない
- 2026-08-25 | qa | 憲章の機械チェックは「アニメーションが静止してから」測る（helpers.settle） | チップの色遷移（150ms）の途中値を拾って 96回に1回コントラスト不足と誤判定していた。実 UI の問題ではなく計測側の問題
- 2026-08-25 | ui | 検索結果の順位欄には「元のランキングでの順位」を出す（検索結果の連番を出さない） | 連番だと1件目が常に金色の1位に見え、意味の無い数字がいちばん目立つ。実順位なら「全期間5位の動画」と読める
- 2026-08-25 | ui | 検索タブの期間チップを独立した行に（部門セグメントと同居させない） | 360px では期間の行が 154px しか取れず、6個中2個しか見えなかった。独立行で 360px を使い5個が一目で入る
- 2026-08-25 | collect | 薄いリストの1ページ買い足しを全ジョブに適用（従来は top24h のみ）。あわせて「前回20件以上あったものが半分未満に減ったら上書きしない」ガードを追加 | 実データで US の月間ランキングが3件で公開されていた（動画部門はショートを除くぶん目減りする）。買い足しは予算に余裕があるときだけ働く
- 2026-08-25 | ui | よく見るチップ行から現在地を除外するのをやめ、「いまここ」として無効化した印を出す | 除外すると軸を移るたびに行が出入りし、リスト全体が 54px 跳ねていた。実測で跳ね 0px に
- 2026-08-25 | ui | 未収集の軸を**隠す**のをやめ、点線＋「…」＋ツールチップの「集計中」として必ず出す。押した軸から勝手に戻すのも起動時だけに限定 | 発注者のスクショ指摘: 年間・全期間とカテゴリが消えて「機能が削除された」ように見えていた。隠すより「これから埋まる」と伝えるほうが正直で、押した先では pending の案内が出るので 404 の誤解も起きない

- 2026-08-25 | order | 第3弾の発注者口頭改訂を ORDER に注記して実装: 24h＝毎日1回でよい／国別強化（IN・BR追加＝6カ国、地図60カ国、増枠後 FR・DE で8カ国）／全期間は2005年まで遡り収集／グループランキング新設／増枠は20,000で申請 | 「リアルタイムは3時間ごとでも」という当初案は、確認の結果「24時間ごとでよい、その分国別を強く」に確定した
- 2026-08-25 | quota | 増枠（ゲートF）は20,000で申請。YouTube Data API に有料の追加割当は存在しない（従量課金メニューが無い）ことを BUDGET.md に明記し、申請文面を SUBMISSIONS.md §5 に用意 | 発注者の「課金するならいくら？」への答えは「0円・無料の審査申請のみ・数週間待ち」。申請には公開の利用規約が必要なので public/terms.html を新設した
- 2026-08-25 | collect | 全期間バックフィル: 年窓 search（publishedAfter/Before・2005年〜・ショートは2020年〜）→候補プール state/pool/{国}.json.gz →videos.list で実数→year/all をプールから 0 units で再構成。予約枠は planSchedule の reservedUnits（ソフト上限から差し引くだけ）で実現し、完走すると自動で解除→浮いた枠は planner が週間などへ自動昇格 | ランキング定義が「期間内投稿の再生数順」なので過去の窓を切れば遡れる。ORDER §8 は毎日の poolrefresh（50件=1unit）と retention の30日削除で満たす。プールは冪等（キャッシュ消失時は歩き直すだけ）
- 2026-08-25 | collect | 「どのジョブも1回の費用 < 1日のハード停止」を不変条件にし、catweekmonth / catyearall を期間単位の catweek / catmonth / catyear / catall に分割した | 6カ国化で1回12,120 units となり、1周を1回の実行で完走できず lastRun が永遠に進まない＝常に due のまま予算を食い続けて年間・全期間を飢餓させることが60日シミュレーションで発覚（再開カーソルでは防げない種類の飢餓）。テストで固定
- 2026-08-25 | collect | top24h は desiredHours=24 のキャップで毎日に固定し、floorHours=168 で緊急時（割当激減）は遅くして生かす | 発注者意向「当面毎日でよい」の実装。floor=24 のままだと低割当時に降格の逃げ場が無く、スキップで「消える」挙動になった
- 2026-08-25 | groups | グループランキングは workers/taps に相乗り（同一デプロイ・同一KV）。認証なしの capability URL（乱数10文字）＋多層上限（200本/グループ・300追加/日・作成100/日・IPスロットルはメモリ内のみ）＋自由文を受け付けない設計 | ORDER §2-15 の「個人情報が仕組み上存在しない」原則をグループにも拡張。§2-17 の文字コミュニティ（保留）とは別物であることを ORDER §2-18 に明記。題名は youtube.com/oembed（0 units・CORS 非対応のため Worker 中継）で解決
- 2026-08-25 | tags | SEARCH_Q の常用語（pt/fr/de/hi）をストップワードに追加し、ハングルとデーヴァナーガリーの語抽出を実装 | 検索語は全動画のタイトルに載るため、入れないと各国のワード1位を「de」「के」が占拠する。KR のワードタブがハングルを1語も拾えていなかった既存の穴も同時に塞いだ
- 2026-08-25 | e2e | taps の E2E ヘルパー routeConfigWithEndpoint を TAPS ブロック限定の置換にした | GROUPS.endpoint の新設で `endpoint: ''` が2箇所になり、最初の出現を書き換える旧実装は GROUPS 側を書き換えて taps のテストが静かに無意味になった
- 2026-08-26 | budget | 実行順を「デイリー枠（everyHours≤24＝top24h/map）を先頭 → 残りは従来のエイジング」の2段構えに変更 | 第3弾公開翌日、未実行ジョブ（overdue=∞）約37,000units が top24h を追い越し、「24hは毎日1回」（ORDER 第3弾改訂①）が1日破れた実測より。デイリー枠は計2,484units と安く、他ジョブの回復を妨げない
- 2026-08-26 | budget | 周回（ラップ）を実行またぎで数える: 予算切れで止まったジョブは次の実行で「残りのリストだけ」を歩いて完走を閉じる（state の lastrun に lap を追加。旧形式は新しい周回として読む） | 従来は再開のたび1周まるごと歩き直すため、「1回で歩き切れない予算しか無い日」が続くジョブは同じリストへ割当を二重払いしながら永久に完走せず、∞-overdue のまま後続を飢餓させる（バックフィル予約1,440/日の60日シミュレーションで実測。実データでも今日 catweek が該当）
- 2026-08-26 | budget | canSpend に「デイリー枠の取り置き」を追加: 今日のクォータ日にまだ完走していない everyHours≤24 ジョブの費用ぶんは、他のジョブに使わせない | 実行順の並べ替えは1回の実行内でしか効かない。top24h の期限が日の途中に来る日、先に走った未消化の山が残額を使い切ると当日の24h更新が飛ぶ（多角レビューで指摘・シミュレーションの負対照で「1日飛ぶ」を実測）
- 2026-08-26 | budget | 書き損ね（0件・激減の据え置き）で閉じた周回の取り直しは1回まで。2周連続で駄目なら lastRun を進めて次の間隔まで諦める | 慢性的に書けないリストが1本あると永久に完走せず、毎時の実行がリング1周ぶんの割当を払い続けて他を飢餓させる（top24h なら 2,424×4周 ≈ 9,700 > ハード停止9,500。多角レビューで指摘）
- 2026-08-26 | budget | 周回（lap）にリングの署名（国・部門・期間・カテゴリの djb2 ハッシュ）を持たせ、不一致なら残数を捨てて新しい周回にする | 残数だけだと国の追加・入替のデプロイ後に「新しい国のリストを取らないまま完走扱い」になる（FR/DE 追加が予定されており実際に踏む。多角レビューで指摘）

## 2026-08-30 公式急上昇（chart=mostPopular）の毎時合流

- **発端**: 発注者側「今日出てる200万回再生くらいの動画がランキングにないことが結構ある。公式も混ぜたい」。
  実測で裏付け: 急上昇（map ジョブの JP top10）に 3,634,585回 の動画がいるのに、
  JP-video-24h-all の1位は 1,256,890回（2026-08-30 16:24 JST の快照）。
- **原因は3層**: ①本体ランキングは search.list 頼みで、search の索引は「期間×再生数順」を網羅しない
  ②24h ランキングは第3弾の発注者改訂で毎日1回（最大24時間古い） ③SEARCH_Q の言語語で数%落ちる。
- **対策**: `chart` ジョブ新設（priority 0・毎時・6国×1 unit=6 u/回・144 u/日）。
  chart=mostPopular 50件/国を取り、**追加と統計更新だけ**を 24h/週間/月間へ合流
  （scripts/lib/chart.mjs の mergeChartIntoList・純関数）。年間・全期間へは freshByCountry →
  feedPools → rebuildFromPool の既存経路で届く。search がまだ作っていないリストには作らない
  （chart 50本だけの薄いリストを公開しない）。窓より古い投稿は 24h に混ぜない。
- **同日の多角レビュー（4視点＋反証2名）で確定した指摘3件を、公開前に修正した**:
  ① **search の完走が chart の行を消していた**。runListJob は search の生結果でファイルを丸ごと
     上書きするので、chart だけが知る動画（＝search の索引が返さない＝この改修の存在理由）が
     top24h/weekmonth の完走のたびに消えていた。急上昇の寿命（数日）は週間・月間の窓（7/30日）
     より短いので、一度消えた大物は窓の残り期間ずっと戻らない（プール再構成は year/all しか
     書かないので復元経路も無い）。→ **書き手が2つあるリスト（24h/週間/月間×総合）は
     search 側も同じ合流規則を通す**（`isMultiWriterList` / `mergeIntoList`）。
  ② **激減ガードの基準が chart で汚染されていた**。`prevCount` は公開ファイルの件数なので
     chart が足したぶん系統的に上振れし、search の収量が薄いリスト（shorts 部門は買い足し対象外）
     では恒久的に据え置き→毎回 dirty lap で周回を取り直し→割当を空費する経路があった。
     → ガードは**合流後の件数**で見る（①の修正で件数が collapse しなくなり、実質ほぼ発火しない）。
  ③ BUDGET.md の増枠表・目安表が chart 追加前の数値のままだった → 実測値に更新。
- **本番の実走で分かって直したこと**: 最初は chart の毎時合流でも「窓の外に出た行」を剪定したが、
  実データで **JP 24h が 84 → 45 件に痩せた**（top24h は毎日1回なので、次の完走まで細り続ける）。
  発注者の関心は「漏れ」なので細らせない。**剪定は search の完全更新のときだけ**（`prune: true`）に
  絞った＝行の古さは改修前と同じ範囲に収まり、件数は保たれる。
  入れるときは厳しく（日付が読めない fresh は入れない）、残すときは寛容に
  （日付が読めない既存行は落とさない＝取りこぼしで痩せさせない）。
  ↑↓NEW の比較元（prev）は書くたびに進むので、上位の矢印は「前回の毎時更新から」の意味になる
  （毎日1回時代の「昨日から」より細かい）。
- **予算**: 定常 7,822/日（78.2%）・バックフィル併走中 7,981/日（79.8%）。どちらも degraded なし。
  chart を足したぶん yearall の search 取り直しが 14日→30日 に自動降格（実害なし・BUDGET.md 参照）。
- **見送り**: 「数日前投稿で今日伸びている動画を 24h に載せる」は 24h の定義（投稿24時間以内）の
  変更になるので発注者確認が要る。週間には chart 合流で載るようになった。
